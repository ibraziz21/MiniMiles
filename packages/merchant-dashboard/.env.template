# Supabase — server-only (never expose to browser)
SUPABASE_URL=""
SUPABASE_SERVICE_KEY=""

# iron-session secret — min 32 chars random string
SESSION_SECRET=""

# App URL (used for CORS / email links)
NEXT_PUBLIC_APP_URL="http://localhost:3001"

# Email notifications — Resend (https://resend.com)
RESEND_API_KEY=""
MERCHANT_NOTIFICATION_FROM="noreply@yourdomain.com"

# Internal webhook secret (shared with react-app for new-order notifications)
INTERNAL_WEBHOOK_SECRET=""
